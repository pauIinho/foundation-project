{% if app.request.hasPreviousSession %}
    {% for type, messages in app.session.flashbag.all() %}
        {% for message in messages %}
            <script>
                $.notify('{{ message }}', {position: 'bottom right', className: '{{ type }}'});
            </script>
        {% endfor %}
    {% endfor %}
{% endif %}